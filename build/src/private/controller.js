(function() {
  var PrivateController, dependencies;

  dependencies = [];

  PrivateController = function() {
    var vm;
    vm = this;
    return vm;
  };

  angular.module('private').controller('PrivateController', dependencies.concat(PrivateController));

}).call(this);
